rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // DEV MODE: Security disabled for testing
      allow read, write: if true; 
    }
  }
}
